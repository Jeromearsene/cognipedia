---
/**
 * About page content — shared between /a-propos (FR) and /about (EN).
 */
import { m } from "@/paraglide/messages";
---

<p class="py-12 text-center text-text-secondary">{m.about_coming_soon()}</p>
